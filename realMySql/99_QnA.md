### MyISAM VS innoDB
### 인덱스 스킵 스캔 VS 인덱스 루스 스캔
### Execution Plan Extra : Using join buffer(batched key access) VS using MRR
### `MAX(from_date) OVER(PARTITION BY emp_no)` VS `MAX(from_date) FROM salares GROUP BY emp_no` (2권, p.137)
